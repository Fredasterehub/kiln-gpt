# Kiln GPT — Naming Decision

## Decision

Working name: `Kiln GPT`

The user explicitly de-prioritized naming.
I am treating this as settled enough for implementation.

## Naming System

Use the simplest viable form:

- Project name: `Kiln GPT`
- Plugin namespace: `kiln`
- Voice in copy: `Kiln`
- Internal architecture references: `Kiln runtime`, `Kiln bridge`, `Kiln kernel`

If a better name emerges later, it should replace the label, not delay the build.
